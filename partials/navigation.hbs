<!--<div class="nav">-->
    <!--<h3 class="nav-title">菜单</h3>-->
    <!--<a href="#" class="nav-close">-->
        <!--<span class="hidden">关闭</span>-->
    <!--</a>-->
    <!--<ul>-->
        <!--{{#foreach navigation}}-->
            <!--<li class="nav-{{slug}}{{#if current}} nav-current{{/if}}" role="presentation"><a href="{{url absolute="true"}}">{{label}}</a></li>-->
        <!--{{/foreach}}-->
    <!--</ul>-->
    <!--<a class="subscribe-button icon-feed" href="{{@blog.url}}/rss/">订阅</a>-->
<!--</div>-->
<!--<span class="nav-cover"></span>-->

<style type="text/css">

    .navbar-default {
        background-color: #fff;
        border-color: #e7e7e7;
        -webkit-box-shadow: 0 2px 5px 0 rgba(0,0,0,0.16), 0 2px 10px 0 rgba(0,0,0,0.12);
        box-shadow: 0 2px 5px 0 rgba(0,0,0,0.16), 0 2px 10px 0 rgba(0,0,0,0.12);
    }
    .navbar-default .navbar-nav>.active>a, .navbar-default .navbar-nav>.active>a:focus, .navbar-default .navbar-nav>.active>a:hover {
        color: #565a5f;
        background-color: #fff;
        -webkit-transition: 0.2s ease;
        -moz-transition: 0.2s ease;
        -ms-transition: 0.2s ease;
        transition: 0.2s ease;
    }
    .navbar-default .navbar-nav>.active>a:hover{
        text-decoration: underline;
        color: #38b7ea;
    }

    @media (min-width:760px)  {
        .container-fluid {
            padding-left: 5em;
            padding-right: 5em;
        }
    }

    .nav-link {
        font-size: 1.6rem;
    }

    .navbar-brand {
        color:#178fe6;
        font-weight: bold;
    }
    .navbar-brand img{
        width: 30px;
        height: 30px;
        border-radius: 50%;
        margin-right: 5px;
    }

</style>

<nav class="navbar navbar-default navbar-fixed-top">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">移动端菜单导航</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <!--{{#if @blog.logo}}-->
                <!--<img class="nav-logo" alt="{{@blog.title}}" src="{{@blog.logo}}">-->
            <!--{{/if}}-->
            <a class="navbar-brand" href="{{@blog.url}}" style="display: flex;align-items: center;">
                <img src="http://iconfont.alicdn.com/t/1494395652678.png@200h_200w.jpg" alt="">{{@blog.title}}
            </a>
        </div>

        <div class="collapse navbar-collapse navbar-right" id="navbar-collapse-1">
            <ul class="nav navbar-nav">
                {{#foreach navigation}}
                    <li class="nav-link{{#if current}} active{{/if}}" role="presentation">
                        <a href="{{url absolute="true"}}">{{label}}</a>
                    </li>
                {{/foreach}}
            </ul>
        </div>
    </div>
</nav>
